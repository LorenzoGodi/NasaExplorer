<Page
    x:Class="NasaExplorer.SpecApodPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NasaExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image x:Name="myimage" Margin="15,15,15,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <RelativePanel Grid.Row="1">
            <DatePicker x:Name="mydate" RelativePanel.AlignHorizontalCenterWithPanel="True" Margin="15" DateChanged="DatePicker_DateChanged"/>
            
        </RelativePanel>

    </Grid>
</Page>
